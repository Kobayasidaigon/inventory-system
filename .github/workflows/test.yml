name: テスト

# ブランチへの push と Pull Request で走らせる。
# デプロイ前に壊れていることに気づくのが目的。
on:
  push:
    branches-ignore:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: npm test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      # 注意: 本番 (Dockerfile) は Node 18 だが、ここは 22 を使う。
      # テストが複数の Set-Cookie を読むのに Headers.getSetCookie() を
      # 使っており、これは Node 20 以降にしかないため。
      # アプリ本体は Node 18 でも動く。
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm

      # puppeteer は devDependency に入っているが、テストはブラウザを使わない
      # （解析部分は jsdom で確かめる）ので Chromium は落とさない
      - run: npm ci
        env:
          PUPPETEER_SKIP_DOWNLOAD: 'true'

      - run: npm test
