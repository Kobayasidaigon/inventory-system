# 別サイトからログインなしで入る（入場リンク）

清掃管理表など、こちらで信頼している別のサイトから、在庫システムへログインなしで
入れるようにする仕組みです。

```
清掃管理表（ログイン済み）
   │  署名付きリンクを作って表示
   ↓
/enter?loc=..&user=..&exp=..&nonce=..&sig=..
   │  署名・期限・使い捨てを確認
   ↓
在庫システム（その拠点の利用者としてログイン済み）
```

## 先に知っておいてほしいこと

**「どこから来たか」では判定していません。** ブラウザが送る `Referer` は送る側が
自由に書ける値で、`curl` 一行で偽装できます。認証には使えません。

代わりに、両サイトで同じ合言葉を持ち、それで作った署名を確かめています。署名を
作れるのは合言葉を知っているサーバーだけなので、偽装できません。

**強さは「清掃管理表のログインと同じ」までです。** 清掃管理表に入れる人は、在庫
システムにも入れるようになります。そういう関係を結ぶということなので、清掃管理表
側のログインが弱いなら、この仕組みは入れない方がよいです。

## 1. 合言葉を決める

長いランダムな文字列を作り、**両方のサーバー**に同じ値を設定します。

```bash
# 値を作る
openssl rand -base64 32

# 在庫システム側
flyctl secrets set LINK_SECRET=（作った値）
```

清掃管理表側にも同じ値を、環境変数などで渡してください。
**コードに直接書かないでください。** このリポジトリは公開されています。

`LINK_SECRET` が未設定の間、この経路は動きません（普通のログイン画面に回ります）。
設定漏れのまま誰でも入れる状態になるより、動かない方が安全なためです。

## 2. 清掃管理表側でリンクを作る

`loc`（拠点コード）と `user`（利用者 ID）を、在庫システムに登録してあるものに
合わせます。管理画面の拠点一覧と利用者一覧で確認できます。

Node.js の場合:

```js
const crypto = require('crypto');

function buildEntryUrl(baseUrl, { loc, user }, secret) {
    const params = {
        loc,
        user,
        exp: Math.floor(Date.now() / 1000) + 300,   // 5 分後まで
        nonce: crypto.randomBytes(16).toString('hex')
    };

    // 並び順もこの通りに。値は URL エンコードしてから連結する
    const canonical = ['loc', 'user', 'exp', 'nonce']
        .map(key => `${key}=${encodeURIComponent(params[key])}`)
        .join('&');

    const sig = crypto.createHmac('sha256', secret).update(canonical).digest('hex');

    return `${baseUrl}/enter?${canonical}&sig=${sig}`;
}
```

**ページを開くたびに作り直してください。** 作り置きして使い回すと、期限と使い捨ての
意味がなくなります。

在庫システム側の `server/services/entry-link.js` に同じ計算をする `buildEntryUrl()`
があります。食い違ったときは、そちらが正です。

## 3. 確認する

清掃管理表にリンクを置いて、踏んでみてください。うまくいけば在庫システムの画面が
そのまま開きます。アドレス欄からは署名付きの部分が消えます（`/` に送り直すため）。

入れなかったときは、在庫システムのログに理由が出ます。

```bash
flyctl logs | grep 入場リンク
```

| ログ | 原因 |
|---|---|
| `LINK_SECRET が未設定` | 在庫システム側に合言葉が入っていない |
| `リンクが正しくありません` | 両側の合言葉が違う、または署名の作り方が違う |
| `リンクの有効期限が切れています` | 作ってから 5 分以上経っている |
| `リンクの有効期限が長すぎます` | `exp` を先に取りすぎ（上限 10 分） |
| `使用済みのリンク` | 同じリンクを 2 回踏んだ。作り直しが必要 |
| `拠点が見つからない` | `loc` が在庫システムの拠点コードと合っていない |
| `利用者が見つからない` | `user` がその拠点の利用者 ID と合っていない |
| `管理者はリンクからは入れません` | 仕様。下記参照 |

## 決めごとと、その理由

| 決めごと | なぜ |
|---|---|
| 有効期限は既定 5 分、上限 10 分 | リンクは履歴にも残る。長生きさせない |
| `exp` が先すぎるものは署名が正しくても拒否 | これを見ないと `exp=2099年` のリンク 1 本で永久パスになる |
| 同じリンクは 1 回しか通らない | 履歴やチャットに残った URL を後から使われないため |
| 成功したら `/` に送り直す | 署名付き URL をアドレス欄・履歴・次の遷移の Referer に残さない |
| **1 年の自動ログインは付けない** | 5 分のリンクから 1 年の資格情報が生えるのは筋が通らない。セッションだけ作る |
| 管理者はこの経路で入れない | リンク 1 本で全拠点を触れる状態を作らないため |

## 関連: QR コードのトークンについて

これとは別に、`?qr_token=` という古い仕組みが残っています（`server/app.js`）。
こちらは**1 年間有効で、何度でも使えます**。URL を知っている人は誰でも、その利用者
として入れます。棚に貼る QR コード用に作られたものです。

入場リンクとは強さがまったく違います。**URL が実質パスワード**なので、扱いには
注意してください。
